description "{{ project_name }}-{{ role_name }}"

{% if blockchain_vote %}
    {% if is_development_environment %}
start on vagrant-mounted
    {% else %}
start on startup
    {% endif %}
{% endif %}
stop on shutdown

start on started {{ project_name }}-blockchain-miner
stop on stopped {{ project_name }}-blockchain-miner

expect fork
respawn

script
    # nothing
end script
