{
  "name": "cocorico-webhook",
  "version": "0.0.1",
  "private": true,
  "license": "MIT",
  "dependencies": {
    "amqplib": "^0.4.2",
    "babel-cli": "^6.14.0",
    "babel-plugin-transform-async-to-generator": "^6.16.0",
    "babel-polyfill": "^6.16.0",
    "babel-preset-es2015": "^6.14.0",
    "bunyan": "1.8.0",
    "superagent": "^2.2.0",
    "superagent-no-cache": "^0.1.0"
  },
  "scripts": {
      "build": "rm -rf dist ; babel src --out-dir dist --source-maps inline",
      "start": "service cocorico-webhook stop ; nodemon dist/index.js -w src -d 5 --exec 'npm run build && nodejs'",
      "test": "eslint ."
  },
  "devDependencies": {
    "babel-eslint": "^6.1.2",
    "eslint": "^3.6.0"
  },
  "babel": {
    "presets": [
      "es2015"
    ],
    "plugins": [
      "transform-async-to-generator"
    ]
  }
}
